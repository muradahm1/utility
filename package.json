{
  "name": "getcalcu",
  "version": "1.0.0",
  "description": "GetCalcu — free online calculators and utility tools",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "build:css": "node scripts/minify-css.js",
    "build:seo": "node scripts/minify-css.js && node build-seo.js",
    "build": "node scripts/minify-css.js && node build-seo.js"
  },
  "devDependencies": {
    "jsdom": "^25.0.0",
    "sharp": "^0.35.4",
    "vitest": "^3.0.0"
  }
}
